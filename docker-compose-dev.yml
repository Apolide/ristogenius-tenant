services:
  
  ristopilot-tenant_database:
    image: mysql:8.0
    command: --default-authentication-plugin=mysql_native_password
    container_name: ${COMPOSE_PROJECT_NAME}_database
    ports:
      - "${DATABASE_PORT_HOST}:${DATABASE_PORT_CONTAINER}"
    environment:
      MYSQL_ROOT_PASSWORD: ${DATABASE_ROOT_PASSWORD}
      MYSQL_USER: ${DATABASE_USER}
      MYSQL_PASSWORD: ${DATABASE_PASSWORD}
      MYSQL_DATABASE: ${DATABASE_DATABASE}
    networks:
      - ristopilot-tenant


  ristopilot-tenant_database_web_ui:
    image: phpmyadmin/phpmyadmin:${DATABASE_WEB_UI_IMAGE_VERSION}
    container_name: ${COMPOSE_PROJECT_NAME}_database_web_ui
    ports:
      - "${DATABASE_WEB_UI_PORT_HOST}:${DATABASE_WEB_UI_PORT_CONTAINER}"
    environment:
      PMA_HOST: ristopilot-tenant_database
      UPLOAD_LIMIT: 300M
    depends_on:
      - ristopilot-tenant_database
    networks:
      - ristopilot-tenant


  #PHP
  ristopilot-tenant_php:
    build:
      context: .
      dockerfile: php-dev-dockerfile
    container_name: ${COMPOSE_PROJECT_NAME}_php
    restart: unless-stopped
    tty: true
    depends_on:
      - ristopilot-tenant_database
    environment:
      - DB_HOST=${DATABASE_HOST}
      - DB_PORT=${DATABASE_PORT_CONTAINER}
      - DB_USERNAME=${DATABASE_USER}
      - DB_PASSWORD=${DATABASE_PASSWORD}
      - DB_DATABASE=${DATABASE_DATABASE}
    volumes:
      - ./code:/var/www/html
      - ./certificates:/var/www/html/certificates
    networks:
      - ristopilot-tenant

  #Nginx
  ristopilot-tenant_nginx:
    image: nginx:alpine
    container_name: ${COMPOSE_PROJECT_NAME}_nginx
    restart: unless-stopped
    tty: true
    ports:
      - "8002:80"
    volumes:
      - ./docker-files/development/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./docker-files/development/nginx/general.conf:/etc/nginx/conf.d/default.conf
      - ./docker-files/development/nginx/includes/:/etc/nginx/includes/
      - ./code:/var/www/html
      - ./certificates:/etc/nginx/ssl/live/ristopilot.com
    networks:
      - ristopilot-tenant
    depends_on:
      - ristopilot-tenant_php

  ristopilot-tenant_redis:
    image: redis:6.2.6
    container_name: ${COMPOSE_PROJECT_NAME}_redis
    restart: unless-stopped
    command: redis-server --include /usr/local/etc/redis/redis.conf
    volumes:
      - ./docker-files/development/redis/redis.conf:/usr/local/etc/redis/redis.conf
    networks:
      - ristopilot-tenant

  ristopilot-tenant_mailhog:
    platform: linux/x86_64
    image: mailhog/mailhog
    container_name: ${COMPOSE_PROJECT_NAME}_mailhog
    logging:
      driver: 'none'
    ports:
      - "8103:1025"
      - "8100:8025"
    networks:
      - ristopilot-tenant      

#Networks
# networks:
#   ristopilot-tenant:
#     driver: bridge
#     name: ristopilot-tenant_network

networks:
  ristopilot-tenant:
    name: ristopilot-tenant_network
    driver: bridge
    ipam:
      config:
        - subnet: 10.250.51.0/24
          gateway: 10.250.51.1


#Volumes
volumes:
  dbristopilottenantsaas:
    driver: local
